export function PrivacyStrip(): React.JSX.Element {
  return (
    <aside className="privacy-strip">
      <strong>Private by design</strong>
      <span>
        PDF contents and filenames stay in this browser tab&apos;s memory. They
        are never uploaded, stored on a server, logged, or included in contact
        messages.
      </span>
    </aside>
  );
}
